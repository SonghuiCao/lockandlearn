<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"

    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="16dp"
    android:paddingRight="16dp" >


    <ImageView
        android:id="@+id/lockimage"
        android:layout_width="300dp"
        android:layout_height="350dp"
        android:layout_centerHorizontal="true"
        android:contentDescription="locklogo"
        app:srcCompat="@drawable/padlock" />

    <TextView
        android:id="@+id/title"
        android:layout_below="@id/lockimage"
        android:layout_centerHorizontal="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="50dp"
        android:text="Lock and Learn"
        android:textColor="@android:color/darker_gray"
        android:textSize="35sp" />

    <Button
        android:id="@+id/playflashcardsbutton"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/title"
        android:layout_centerHorizontal="true"
        android:backgroundTint="@android:color/darker_gray"
        android:text="Play Flash Cards" />
    <Button
        android:id="@+id/createflashcardsbutton"
        android:layout_below="@id/playflashcardsbutton"
        android:layout_centerHorizontal="true"
        android:backgroundTint="@android:color/darker_gray"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:text = "Create Flash Cards"/>
    <Button
        android:id="@+id/settingsbutton"
        android:layout_below="@id/createflashcardsbutton"
        android:layout_centerHorizontal="true"
        android:backgroundTint="@android:color/darker_gray"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:text = "Settings"/>


</RelativeLayout>